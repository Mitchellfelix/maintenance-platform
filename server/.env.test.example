DATABASE_URL="postgresql://maintenance:maintenance@localhost:5432/maintenance_platform_test"
JWT_SECRET="test-secret"
CORS_ORIGIN="http://localhost:5173"
PORT=3001
NODE_ENV=test

# IMPORTANT: Never point this at the live database name `maintenance_platform`.
# Integration tests wipe every table between cases. Use only a dedicated *_test DB.
