DATABASE_URL="postgresql://maintenance:maintenance@localhost:5432/maintenance_platform"
JWT_SECRET="change-me-in-production"
CORS_ORIGIN="http://localhost:5173"
PORT=3000
NODE_ENV=development

# Optional: point the desktop app at a shared deployment instead of localhost.
# When set to a remote URL, the Dock app skips local server startup and loads this URL
# in its own window (no system browser).
# EMAT_APP_URL="https://emat.yourcompany.internal"
